name: CI

on:
  pull_request:
  push:
    branches:
      - update

jobs:
  test_sorts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Make
        run: make

      - name: Run tests
        run: ./x/test_sorts -g
